import io

class identifier
(
    val name: string
    val phone: i32
)

class person
(
    val ident: identifier
    var age: i32
)

fun main() -> i32
{
    val p: person

    p.age = 80

    p.ident.phone = 3

    when (p.age >= 65 || p.ident.phone < 4 && false)
    {
        print("you can retire!\n")
    }

    return p.ident.phone
}